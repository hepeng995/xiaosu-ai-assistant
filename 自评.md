# 自评.md

## 我做了什么，解决谁的什么问题

开发了公司内部 AI 助手「小苏」：员工在钉钉提问 → RAG 知识库回答（带引用/拒答）+ 自研 Tool Calling Agent 查内部数据 + Web 后台管理。解决了员工查制度/查数据需人工、无统一入口的问题。

## 最得意的一个技术决策

**无 API Key 也能完整验证全流程**：embedding/LLM 在 `key=replace_me` 时走确定性 mock（mock 向量、mock 工具选择），检索用字符重叠近似。这样不依赖真实密钥即可验证上传→索引→检索→引用→拒答→工具调用→多轮→IM 兜底的完整链路，大幅降低开发与面试演示门槛。

## 一个被迫的妥协

mock 模式下 LLM 回答是"工具结果摘要"而非自然语言（真实语义需配置 API Key）。工具选择的 mock 启发式是测试替身，真实模式由 function calling 自主决定（无 if-else）。

## 如果再给我一周

1. 接入真实 LLM API Key，做一次端到端真实 function calling + 真实语义检索验收。
2. 钉钉沙箱真实联调（当前为 mock webhook）。
3. 补 RAG 拒答/同名替换的集成测试（连真实 pgvector）。
4. 前端引入 shadcn/ui 统一组件体系。
5. 接入 Langfuse/OpenTelemetry 可观测性 + Evals 评测脚本。
